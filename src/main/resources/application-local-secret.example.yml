# Git에 올리지 않는 로컬 전용 파일 예시다.
# 이 파일을 복사해서 application-local-secret.yml 로 사용한다.
# PowerShell 예시:
# [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("32bytes-or-more-secret-value-here"))

jwt:
  secret: "Base64-encoded-32bytes-or-more-secret"
